<h1>Patients#index</h1>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Phone</th>
    </tr>
  </thead>
  <tbody>
    <% @patients.each do |p| %>
    <tr>
      <td><%= p.name %></td>
      <td><%= p.mobile_phone %></td>
    </tr>
    <% end %>
  </tbody>
</table>
